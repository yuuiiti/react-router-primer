import React from 'react';

class Math extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            count: 0
        };
    }

    add(number) {
        this.setState(() => {
            return count
        });
    }
}

const add = (num) => { 
    return num + num
};

add(1)
